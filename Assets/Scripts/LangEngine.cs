using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LangEngine : MonoBehaviour
{
    public static int Lan_ID;
    // Start is called before the first frame update
    void Start()
    {
        
    }
    public void SetLanID(int id)
    {
        Lan_ID = id;
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
